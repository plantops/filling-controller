# SP01 Progress

Updated: 2026-09-09

Current version marker: **`0.1.0-rc1`**. The `v0.1.0-rc1` tag remains the frozen RC baseline; `main` contains post-RC design/document/CI updates.

Current maturity: **READY FOR FIRST PHYSICAL BOARD BENCH**.

## Project mission

The commercial controller is discontinued / difficult to source while the mechanical rotary packer remains a high-value usable asset. This is an **obsolescence-rescue / asset-life-extension** controller.

Locked lifecycle principle:

> **DESIGN FOR REPLACEMENT, NOT IMMORTALITY.**

A low-cost ESP controller may be a consumable service module if it fails safe, is cheap to replace, can be swapped quickly, does not disturb the calibrated weighing chain, and all important configuration/firmware knowledge exists outside the failed board.

Canonical lifecycle document: [`docs/SERVICEABILITY.md`](docs/SERVICEABILITY.md).

## Current hardware reality

```text
ESP32-S3 Waveshare board: IN HAND
actual board photos:        RECEIVED
literal terminal map:       FROZEN
TLB485:                     NOT YET IN HAND
machine authority:          NONE
```

The immediate work is **G1 + G2 bench bring-up**, not TLB commissioning.

## Actual terminal mapping — NOW FROZEN

The real board cover and open-PCB photos received 2026-09-09 confirm:

```text
DIGITAL OUTPUTS                 DIGITAL INPUTS                 POWER
COM GND 8 7 6 5 4 3 2 1        COM GND 8 7 6 5 4 3 2 1       7~36 V  +  -
```

Service edge:

```text
IO1 IO0 RXD TXD SDA SCL GND VCC
CAN:   H L PE
RS485: A+ B- PE
USB / PoE / ANT / BOOT
```

Open board confirms DI1..DI8, DO1..DO8, DGND markings and RS485 `NC/120R` termination selector.

Canonical terminal docs:

- [`docs/BOARD_TERMINALS.md`](docs/BOARD_TERMINALS.md)
- [`docs/assets/BOARD_TERMINALS.svg`](docs/assets/BOARD_TERMINALS.svg)
- [`docs/ONBOARDING.md`](docs/ONBOARDING.md)

The previous onboarding caution about unknown left/right terminal order is no longer applicable to this photographed board revision. The rule remains: **identify by printed name, not by remembered orientation**.

## Beginner onboarding — READY

Detailed non-expert path now exists:

- [`docs/ONBOARDING.md`](docs/ONBOARDING.md) — literal wiring, power, GitHub artifact, flash, boot, DI/DO, stop conditions, evidence;
- [`docs/BOARD_TERMINALS.md`](docs/BOARD_TERMINALS.md) — exact terminal map from the actual board;
- [`docs/assets/BOARD_TERMINALS.svg`](docs/assets/BOARD_TERMINALS.svg) — one-page visual diagram;
- [`docs/FIRST_BOARD_CHECKLIST.md`](docs/FIRST_BOARD_CHECKLIST.md) — printable bench checklist.

First-board sequence:

```text
USB-only power
-> flash approved GitHub artifact
-> verify safe boot with TLB absent
-> verify ALL DO safe OFF
-> 24 V board-power test on dedicated + / -
-> DI dry-contact: INPUT COM <-> switch <-> DIx
-> DO dummy lamp: OUTPUT COM=+24 V, OUTPUT GND=0 V, lamp between +24 V and DOx
-> G2T later
-> TLB G4 when hardware arrives
```

## Summary

| Area | Status | Notes |
|---|---|---|
| Shared C++17 controller core | PASS | Same logic Linux + ESP32 |
| Linux amd64 build/tests | PASS | conformance + smoke |
| ESP32-S3 CI build | PASS baseline | ESP-IDF v5.5.5 |
| GitHub binary artifact | READY | ESP flash bundle generated by Actions |
| Actual terminal map | **FROZEN** | from physical cover + open-board photos |
| Beginner first-board docs | **READY** | exact map + detailed guide + printable checklist |
| MANUAL mode | PASS host | fill-only; bag push hard-blocked |
| AUTO mode | PASS host | continuous fill/discharge sequence |
| Discharge timing | VERY LOW RISK | fixed geometry at fixed 14.4 s/rev; A/B adds local speed measurement |
| Waveshare 8DI/8DO adapter | READY FOR BENCH | physical proof starts now |
| Safe DO initialization | READY FOR BENCH | ALL-OFF boot/reset must be physically verified |
| 70 °C environment | G2T REQUIRED | characterize practical lifetime + fail-safe behavior |
| Replaceable controller strategy | DESIGN LOCKED | independent node + spare + recoverable config |
| Config export/import | PENDING | important for future spare provisioning |
| TLB485 Modbus layer | WAITING FOR HARDWARE | code compiles; real transport not yet measured |
| Weight strategy | LOW RISK | proven operator target recipes |
| Calibration service | READY FOR BENCH LATER | physical 0/20/50 kg waits for TLB/load cell |
| Runtime Settings/recipe write | PENDING | preview UX only; current config via menuconfig |
| SP01 shadow/live authority | NOT STARTED | no machine actuation approved |

## Current risk map

| Risk | Current view |
|---|---|
| FSM / sequence | Low |
| Filling target compensation | Very low |
| Discharge mechanics/timing | Very low |
| Physical terminal identity | Low; map now frozen from actual hardware |
| Physical DI/DO electrical behavior | Medium until G2 evidence |
| TLB485 -> ESP digital weight transport | Medium, waiting hardware |
| 24 V field electrical integration | Medium |
| 70 °C environment | Medium-high lifetime risk, mitigated by replaceability |
| Vendor/controller obsolescence | Primary business risk being removed |
| Long-term ESP board life | secondary if failure is safe and replacement is easy |

## Locked weighing decision

```text
load cell bridge
    -> LAUMAS TLB485
    -> isolated RS485 terminal A+ / B- on ESP board
    -> asynchronous digital WeightSnapshot
    -> controller
```

- continuous weight does not go through DI;
- ESP32 does not read raw mV/V in production;
- all eight DIs remain machine signals;
- control never blocks waiting for Modbus;
- bring-up 9600 bit/s / 50 ms poll;
- target after clean G4 evidence: 115200 bit/s / 20 ms poll (~50 Hz);
- ESP replacement must not automatically write TLB zero/span;
- RS485 `NC/120R` jumper remains untouched until G4 bus topology is frozen.

Canonical design: [`docs/WEIGHING.md`](docs/WEIGHING.md).

## Target adaptation

```text
Recipe 50.0 kg
Recipe 50.1 kg
Recipe 50.2 kg
Recipe 50.3 kg
...
```

External check-scale results guide fast recipe switching. Calibration remains unchanged. No PID/AI auto-target adaptation is required in v0.1.

## 70 °C strategy

Possible ambient near the rotating controller may reach approximately 70 °C.

Success criterion is not a 20-year ESP lifetime. It is:

```text
safe failure / reboot
known output state
measured field temperature
measured practical lifetime
fast spare replacement
recoverable config
```

TLB is a separate service module; relocate/substitute if its verified environment is unsuitable.

## Current gate status

```text
G0   Linux + ESP32 build/conformance             PASS
G1   ESP boot; ALL DO safe OFF                   PENDING — BOARD IN HAND
G2   8 dummy DI / 8 dummy DO loads               PENDING — NEXT
G2T  thermal + serviceability                    PENDING
G3   MANUAL dry fill cycle                        PENDING
G4   TLB485 digital weight over isolated RS485    WAITING FOR TLB HARDWARE
G5   ZERO / 20 kg / 50 kg calibration             WAITING FOR TLB HARDWARE
G6   AUTO dry cycle + discharge timing             PENDING
G7   Wi-Fi loss / reboot / stale / comm faults    PENDING
G8   SP01 shadow                                  PENDING
G9   controlled live SP01 pilot                   PENDING
```

## Immediate next run — physical board now

Follow [`docs/ONBOARDING.md`](docs/ONBOARDING.md) and [`docs/FIRST_BOARD_CHECKLIST.md`](docs/FIRST_BOARD_CHECKLIST.md), with machine completely disconnected.

### Phase 1 — USB only

```text
attach antenna
leave all screw terminals empty
USB-C data cable only
flash approved GitHub Actions artifact
verify boot
verify no reset loop
verify missing TLB stays safe
verify ALL DO remain OFF
```

### Phase 2 — 24 V board power

```text
USB disconnected
bench PSU OFF
PSU + -> dedicated terminal printed +
PSU 0V -> dedicated terminal printed -
set 24.0 V with conservative current limit
power ON
check normal boot/current/temperature
power OFF before changing wires
```

### Phase 3 — DI

```text
INPUT COM -> dry switch -> DI1
INPUT GND unused for passive-contact test
repeat DI1 -> DI8
record physical terminal <-> software identity
```

### Phase 4 — DO

```text
OUTPUT COM -> +24 V
OUTPUT GND -> 0 V
+24 V -> dummy lamp -> DO1
repeat DO1 -> DO8
verify OFF / ON / reset->OFF
no machine solenoids yet
```

A dedicated safe service/bench output-control path is preferred over raw GPIO hacks. Do not bypass the production output ownership model just to make G2 easier.

## G2T later

```text
measure controller location temperature
representative DI/DO + Wi-Fi + RS485 load
reboot / brownout / fault
verify safe outputs
spare-controller replacement drill
restore approved config
```

## G4 when TLB arrives

Record:

```text
0 / 20 / 40 / 49 / 50 kg digital values
update rate
sample age / latency
jitter
Modbus errors
stale detection
disconnect/reconnect
inductive-load noise
elevated-temperature behavior
```

## Release rule

`v0.1.0-rc1` remains a frozen bench release candidate and must not be moved.

Promotion to production `v0.1.0` requires at minimum:

```text
G1..G7 PASS with evidence, including G2T
stable TLB/load-cell transport
verified calibration
verified MANUAL and AUTO behavior
safe restart/fault behavior
known-good spare + documented recovery path
critical config recoverable outside the controller
SP01 shadow review before actuator authority
```

Production success is defined primarily as **removing the discontinued controller as the reason the mechanical packer can no longer operate**.
